// General game logic utilities

export const initializeSnake = () => {
  return [{ x: 0, y: 0 }];
};

export const moveSnake = (snake, direction) => {
  // implement movement
};

export const checkCollision = (snake) => {
  // implement collision detection
};
